package com.java.practice;

public class SQLQuaries {
	
	
	/*
	 * 
	 * select max(salary) from employee where salary < (select max(salary) from employee);
	 * select product_name from product where product_name like 'a%';
	 * 
	 * 
	 * 
	 */

}
